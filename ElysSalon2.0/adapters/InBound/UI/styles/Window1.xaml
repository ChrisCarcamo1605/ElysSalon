<Window x:Class="ElysSalon2._0.adapters.InBound.UI.styles.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElysSalon2._0.adapters.InBound.UI.styles"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <!-- Panel Izquierdo: Lista de Productos -->
        <StackPanel Width="250" Background="#F0F0F0" HorizontalAlignment="Left">
            <TextBlock Text="Productos" FontSize="18" FontWeight="Bold" Margin="10"/>
            <ItemsControl ItemsSource="{Binding ProductList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}" Padding="10" Margin="5"
                                Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Panel Derecho: Carrito de Compras -->
        <StackPanel Width="470" HorizontalAlignment="Left" Margin="138,-10,0,10">
            <TextBlock Text="Carrito de Compras" FontSize="18" FontWeight="Bold" Margin="10"/>
            <ItemsControl ItemsSource="{Binding Cart}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1" BorderBrush="Gray" Padding="10" Margin="5" CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Nombre del Producto -->
                                <TextBlock Text="{Binding Name}" FontSize="14" Grid.Column="0" VerticalAlignment="Center"/>

                                <!-- Cantidad -->
                                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Center">
                                    <Button Content="-" Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                    <TextBlock Text="{Binding Quantity}" FontSize="14" Width="30" TextAlignment="Center"/>
                                    <Button Content="+" Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                </StackPanel>

                                <!-- Precio -->
                                <TextBlock Text="{Binding Price, StringFormat=C}" FontSize="14" Grid.Column="2" VerticalAlignment="Center"/>

                                <!-- Botón de Eliminar -->
                                <Button Content="X" Grid.Column="3" Foreground="Red"
                                        Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>
